fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0
# Windows 与精简容器可能不提供系统 IANA 时区数据库
tzdata>=2025.2
langchain-core>=0.3.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0
langchain-huggingface>=0.1.0
sentence-transformers>=2.6.0
# Windows 本地可装默认 torch；Linux Docker/CI 必须用 requirements.linux-cpu.lock.txt（torch==2.6.0+cpu，无 nvidia-*）。
# 重新生成 Linux CPU 锁（在 Linux 容器或 CI runner 上执行）：
#   bash scripts/regenerate_linux_cpu_lock.sh
# 禁止用仓库根目录 uv.lock 作为服务端锁定证据（T1-05）。
# torch<2.6 存在 torch.load 反序列化 RCE（CVE-2025-32434），sentence-transformers 会从 HF 缓存加载权重，必须 ≥2.6。
torch>=2.6.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0
# RAG 混合检索
rank-bm25>=0.2.2
jieba>=0.42.1
requests>=2.31.0
pypdf>=4.0
numpy>=1.26
httpx>=0.27
python-multipart>=0.0.9
python-magic>=0.4.27
redis>=5.0.0
PyJWT>=2.8.0
bcrypt>=4.1.0
alembic>=1.13.0
aiofiles>=23.2.0
sse-starlette>=2.0.0
# PostgreSQL / PGVector
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0
pgvector>=0.2.0
# 测试 & 评测
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.14
ragas>=0.4.3
httpx>=0.27
faker>=25.0
# 静态检查 / 格式化
ruff>=0.9.0
mypy>=1.14.0
